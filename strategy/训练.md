- 训练脚本是 /D:/project/quant/scripts/build_b1_rank_scores.py:201。
  - 默认做滚动训练：
      - 训练窗 24 个月
      - 验证窗 6 个月
      - 测试窗 3 个月
      - 每次前滚 3 个月
      - 最少训练样本 200
  - 默认后端是 auto，优先用 LightGBM 的 LGBMRegressor，导不进来就退回 HistGradientBoostingRegressor，见 /D:/project/
    quant/scripts/build_b1_rank_scores.py:59。
  - 当前实现不是 Qlib 原生 LGBModel，而是这个本地脚本直接训练 sklearn/LightGBM 管线。

  - 每个滚动测试窗会输出 instrument + datetime + model_score_raw，写到 /D:/project/quant/data/reports/
    b1_rank_smoke_small/b1_model_scores.parquet 这类文件里，代码在 /D:/project/quant/scripts/
    strategy/strategy.py:186。
  - 最终参与交易决策的是融合分：
      - final_score = 0.3 * quality_score + 0.7 * model_score
  - 后面 model_score 还会直接影响：
      - 是否允许确认加仓：model_score >= 60，见 /D:/project/quant/strategy/strategy.py:245
      - 主仓档位：<50 -> 12%，50~80 -> 15%，>=80 -> 18%，见 /D:/project/quant/strategy/strategy.py:439